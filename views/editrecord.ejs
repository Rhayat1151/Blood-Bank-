<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Donor Record</title>
  <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
  <h1>Edit Donor Record</h1>

  <form action="/edit/<%= group %>/<%= donor.id %>" method="POST">
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" value="<%= donor.name %>" required><br>

    <label for="contact">Contact:</label>
    <input type="text" id="contact" name="contact" value="<%= donor.contact %>" required><br>

    <label for="email">Email:</label>
    <input type="email" id="email" name="email" value="<%= donor.email %>" required><br>

    <label for="age">Age:</label>
    <input type="number" id="age" name="age" value="<%= donor.age %>" required><br>

    <label for="last_donated">Last Donated Date:</label>
    <input type="date" id="last_donated" name="last_donated" value="<%= donor.last_donated %>"><br>

    <button type="submit">Update Record</button>
  </form>

  <a href="/blood/<%= group %>">Cancel</a>
</body>
</html>
